# 0405todo

这份文档用于整理：在讨论 `/Users/pifazuoren/Downloads/AgentSociety-main/exp- analysis/04050058.md` 之后，目前代码层面**已经实现了什么**、**还没有实现什么**、以及**哪些内容本来就不该在这一轮先实现**。

判定原则：

- 以 `/Users/pifazuoren/Downloads/AgentSociety-main/exp- analysis/04050058.md` 中暴露出的实验问题为主线
- 同时参考 `/Users/pifazuoren/Downloads/AgentSociety-main/helplessnessupdatetodo.md` 判断这些修改是否符合既定阶段路线
- 这里只讨论“代码状态”和“实验状态”，不讨论论文写法

---

## 一、已实现

### 1. `task appraisal` 近乎常数的问题，已经做了第一轮修正

对应 `04050058.md` 中的问题：

- `felt_control`
- `expected_help_effectiveness`
- `task_value`
- `perceived_task_difficulty`
- `perceived_task_risk`

在运行中几乎都接近常数。

本轮已经做的修改：

- 加细 `task appraisal` 的 cache key，让 `world_context / profile_summary / recent_episode_summary` 更容易进入分流
- 修改 `resolve_task_appraisal(...)` 的 prompt：
  - 增加“不同上下文应输出不同值”的明确要求
  - 把原本高度固定的示例值换成更中性的示例
  - 增加 calibration notes
- 同时加细 `strategy deliberation` 的 cache key，防止后续策略层继续被粗粒度缓存钉住

主要文件位置：

- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/proto/llm_psychology.py`

和 `helplessnessupdatetodo.md` 的对应关系：

- 对应 Stage 2 中“让 `felt_control` 真正成为事件层调节项”的方向
- 也符合“先把中介层接真实，再谈后续更复杂机制”的路线

当前状态：

- 代码已改
- 但尚未重新跑完整实验验证是否真的摆脱常数问题

### 2. seed 没有真正传导的问题，已经修正

对应 `04050058.md` 中的问题：

- `seed=101` 和 `seed=102` 几乎完全一致

本轮已经做的修改：

- agent 内部用于策略采样和 outcome 采样的随机种子，已纳入：
  - `EXP_SEED`
  - `PARALLEL_PAIR_SEED`
  - `WORLD_NAME`

主要文件位置：

- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/proto/agent.py`

和 `helplessnessupdatetodo.md` 的对应关系：

- 对应第 0 阶段和验证原则中的 `paired-seed world comparison`

当前状态：

- 代码已改
- 但尚未通过新实验验证不同 seed 是否已经真正拉开

### 3. `uncontrollability` 在 hybrid 下实际上还是 rule-only，这一默认行为已修正

对应 `04050058.md` 中的问题：

- 实验里 `perceived_uncontrollability` 实际主要还是 rule 在给值

本轮已经做的修改：

- 当 `PROTO_LLM_PSYCHOLOGY_MODE=hybrid` 且没有显式设置 `PROTO_LLM_UNCONTROLLABILITY_MODE` 时，默认让 `PROTO_LLM_UNCONTROLLABILITY_MODE` 跟随为 `hybrid`

主要文件位置：

- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/config_runtime.py`

和 `helplessnessupdatetodo.md` 的对应关系：

- 对应 Stage 1 中“强化 uncontrollability 主导性”的方向

当前状态：

- 默认配置行为已改
- 但尚未验证新的 run 中 LLM calibrator 是否真的更频繁介入

### 4. `support` 长期停留在 `substituting_support`，已经做了第一轮放宽

对应 `04050058.md` 中的问题：

- support 基本还是“代办式”
- 很难进入真正的 `enabling_support`

本轮已经做的修改：

- 调整 `infer_support_mode(...)`
- 降低 `enabling_support` 的进入门槛
- 增加高质量 support 的信号贡献
- 让 `success_with_help` 更容易被识别为 `enabling_support`

主要文件位置：

- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/proto/outcome_model.py`

和 `helplessnessupdatetodo.md` 的对应关系：

- 对应 Stage 5 中 support mode 的区分：
  - `enabling_support`
  - `substituting_support`

当前状态：

- 代码已改
- 但还不是完整的 support 间接修复链

### 5. `controllable_success_memory` 完全不激活，已经做了第一轮修正

对应 `04050058.md` 中的问题：

- 四个 world 中 `controllable_success_memory` 都没有真正被激活

本轮已经做的修改：

- 放宽 `success_self` 的记忆增长条件
- 放宽 `success_with_help` 在 `enabling_support` 下的记忆增长条件
- 让真正具有一定控制感的成功，更容易留下慢速保护性记忆

主要文件位置：

- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/proto/experience_memory.py`

和 `helplessnessupdatetodo.md` 的对应关系：

- 对应 Stage 4 “加入 `controllable_success_memory`”
- 也符合“慢速长期保护项，而不是每次普通成功都快速推高”的原则

当前状态：

- 代码已改
- 但尚未通过新实验验证 memory 是否已经开始出现

### 6. 整体 helplessness 增幅过猛，已经做了第一轮降强度

对应 `04050058.md` 中的问题：

- baseline 也接近全员塌到 `100`
- ceiling effect 太强

本轮已经做的修改：

- 下调 `BASE_DELTAS`
- 下调 `repetition_delta`
- 下调 `efficacy_loss_term / control_loss_term`
- 加强 `damping_factor`
- 调整 `controllable_success_protection`
- 缩小 direct `support_buffer`
- 调整 `mastery_recovery_term`

主要文件位置：

- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/proto/state_update.py`

和 `helplessnessupdatetodo.md` 的对应关系：

- 综合对应 Stage 1、2、5
- 核心方向仍然保持：
  - `uncontrollability` 主导
  - `task_self_efficacy + felt_control` 做中介/调节
  - support 以间接修复为主

当前状态：

- 代码已改
- 但尚未通过新实验验证饱和问题是否明显缓解

### 7. fixed multi-seed baseline / paired-seed 新对照的运行链已补齐

对应你后面单独强调的问题：

- 之前虽然已经有 `analysis_parallel_paired.py`
- 但它更接近旧的三世界 `B-A / C-A` 口径
- `world_runner.py` 也没有一次运行后自动把 paired 结果产出来

本轮已经做的修改：

- `analysis_parallel_paired.py`
  - 从旧的 `baseline + high + low` 固定三世界写法
  - 改成 `baseline vs multiple comparison worlds`
  - 默认可直接覆盖当前四世界：
    - `high_friction_low_assist`
    - `high_friction_high_assist`
    - `low_friction_high_assist`
- `world_runner.py`
  - 新增 `--summarize-paired`
  - 一次 run 完成后可自动输出：
    - manifest
    - config snapshot
    - `parallel_world_summary`
    - `parallel_stage_summary`
    - `parallel_world_paired_diffs`
    - `parallel_world_paired_stats`
    - `parallel_world_paired_qc`
  - 同时新增 baseline / compare world 参数和脚本路径参数，便于固定 baseline 实验复用
- 增加测试，验证：
  - 四世界 summary 能正确产出多 world paired csv
  - `world_runner --summarize-paired` 能把整条输出链跑通

主要文件位置：

- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/analysis_parallel_paired.py`
- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/world_runner.py`
- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/parallel_world.md`
- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/tests/test_parallel_paired.py`

和 `helplessnessupdatetodo.md` 的对应关系：

- 直接对应第 0 阶段里“冻结 baseline，补观测并固化多 seed 对照”
- 也对应验证原则中的 `paired-seed world comparison`

当前状态：

- 运行与汇总链已经补齐
- 但“固定保存的一组新 baseline 结果”本身还没有重新实际跑出来

---

## 二、未实现

### 1. 没有重跑新实验

这是当前最重要的未实现项。

也就是说，现在还不能说：

- `felt_control` 已经不恒定
- `controllable_success_memory` 已经开始激活
- baseline 已经不再快速冲到 `100`
- seed 差异已经真正表现出来

现在最多只能说：

- 代码层面已经做了针对性修正
- 但实验层面还没有完成重新验证

### 2. 没有实现 `04050058.md` 中分析层面的修正

例如：

- `parallel_stage_summary` 和 `parallel_world_summary` 的口径差异问题

这轮并没有修改：

- `/Users/pifazuoren/Downloads/AgentSociety-main/examples/digital_friction_mvp/analysis_parallel_worlds.py`

因此，summary 统计口径本身没有被重构，只是分析时被解释清楚了。

### 3. 没有继续推进 avoid 的强化版本

`04050058.md` 中提到：

- avoid split 已经有了，但仍然偏弱

这轮没有继续实现：

- avoid reason 的更强 hybrid 分类
- avoid 原因的人工抽样核查
- 更完整的 `risk_avoid / low_value_avoid` 强化版分析

所以 avoid 这块基本仍停留在前一轮状态。

### 4. 还没有真正跑出并固定保存一组新的 multi-seed baseline 结果

虽然运行链已经补齐，但这轮还没有实际完成：

- `10+ seeds` 的正式 baseline
- 固定保存可复用的成套结果文件
- 基于新代码重新做 paired-seed 结果解释

因此，现在最多只能说：

- 验证基础设施已经补齐

还不能说：

- 已经得到新的固定 baseline 结果

### 5. 没有完成 `helplessnessupdatetodo.md` 中更完整的验证路线

这轮没有完成：

- ablation
- sensitivity analysis

因此，这一轮仍然是“把下一轮验证跑顺”，不是“完成全部验证闭环”。

### 6. 没有把 support 做成完整的中介修复链

目前 support 仍然只是：

- direct buffer 更小了
- 更容易进入 `enabling_support`
- 更容易帮助积累 success memory

但还没有被完整实现成：

```text
support
-> repair felt_control / task_self_efficacy / understanding
-> then indirectly reduce helplessness
```

这意味着，support 现在只是更接近 todo 里的方向，还不是 fully realized 版本。

### 7. 没有完成 todo 中更完整的消融设计

还没有做：

- `No-Uncontrollability`
- `No-Efficacy-Mediator`
- `No-Avoid-Split`
- `No-Control-Memory`
- `Direct-Support-Only`
- `No-Damping`

所以当前并不能从实验上区分：

- 到底是哪一项修改贡献最大
- 哪一项只是理论上接入了，但实际贡献很小

---

## 三、不该这轮先实现

### 1. 不该一次把整套机制做满

按照 `/Users/pifazuoren/Downloads/AgentSociety-main/helplessnessupdatetodo.md` 的设计，本来就应该分阶段推进。

所以这一轮本来就不该：

- 一次性把全部论文叙事完全落地
- 一次性做完 support / avoid / memory / damping / ablation 的全量版本

更合理的做法，是先修最影响下一轮实验解释力的问题。

### 2. 不该先宣称机制已经“验证成功”

因为当前没有新实验结果，所以这一轮最多只能说：

- 已经实现了修改方向

不能说：

- 已经完成新的实验验证

### 3. 不该优先做过大的 analysis pipeline 重构

当前最重要的是：

- 让下一轮实验更可信

而不是优先重写整套分析代码。

换句话说，这一轮更合理的重点是：

- 修机制输入
- 修中介层
- 修随机性
- 修更新强度

而不是先全面重构 plotting / summary / analysis 逻辑。

---

## 四、当前最准确的状态判断

最准确的总结是：

**目前已经实现了 `04050058.md` 中“最核心、最紧急、最影响下一轮实验解释力”的那一批修改方向；但还没有完成新的实验验证、分析层修复、完整 avoid 强化、完整 support 中介链以及 todo 中要求的消融和敏感性分析。**

换句话说：

- 现在已经不是“还没动”
- 但也绝不是“已经完整实现并验证”

当前更合理的状态描述是：

**完成了第一轮面向问题的代码修正，但尚未完成验证闭环。**
